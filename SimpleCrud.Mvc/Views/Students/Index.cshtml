@model IEnumerable<Student>
<div class="d-flex flex-column justify-content-center">
    <div class="align-self-center" id="indexTitleContainer">
        <h1>Students</h1>
    </div>
    <div class="" id="indexButtonContainer">
        @Html.ActionLink("Create", "Create", "Students", null, new { @class = "btn btn-success" })
    </div>
    <partial name="Partials/_PartialListStudents" model="@Model"/>
</div>
